# cypress-automation
What was the easiest concept to understand?
The easiest concept to grasp was the describe(),it(),.get() and before ,beforeach and after and aftereach blocks. The describe block acts as a logical container for a specific test description.
while the nested it block defines an individual test case,
What was the most challenging?
The most challenging concept was cy.intercept().While functions like cy.get() were easily understood ,the intercepting part was abit challenging.Also learning about .wait() in relation to cy.intercept was also confusing.
